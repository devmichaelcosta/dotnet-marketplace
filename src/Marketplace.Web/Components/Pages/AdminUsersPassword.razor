@page "/admin/users/{Id:guid}/password"
@attribute [Authorize(Roles = "admin")]
@rendermode InteractiveServer

<PageTitle>Resetar senha</PageTitle>

<AdminUserEditor UserId="Id" PasswordOnly="true" />

@code {
    [Parameter]
    public Guid Id { get; set; }
}
